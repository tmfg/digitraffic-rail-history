# Require packages to be at least 7 days old before installation (10080 minutes).
# Protects against compromised packages being used immediately after publish.
minimum-release-age=10080

# Security related fixes - allow specific versions immediately when needed.
# These bypass the 7-day cooldown for urgent CVE fixes.
# Example: minimum-release-age-exclude[]=serialize-javascript@7.0.5

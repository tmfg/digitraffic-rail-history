name: Install
on:
  workflow_dispatch:
    inputs:
      env:
        description: 'Environment'
        required: true
        type: environment
        default: 'beta'
      commit-hash:
        description: 'Commit hash/ECR image tag'
        required: true
        type: string
      config-branch:
        description: 'Config repo branch'
        required: true
        type: string
jobs:
  install:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      actions: read
    environment: ${{ github.event.inputs.env }}
    steps:
      - name: Update task definition
        uses: tmfg/digitraffic-actions@update-task-def/v1
        with:
          family-name: rail-${{ github.event.inputs.env }}-train-history-web
          image-arn: ${{ secrets.ECR_URL }}/train-history-frontend:${{ github.event.inputs.commit-hash }}
          aws-role: ${{ secrets.AWS_ROLE }}
          aws-region: ${{ secrets.AWS_REGION }}
          ci-repo-url: ${{ secrets.CONFIG_REPO_URL }}
          ci-repo-branch: ${{ github.event.inputs.config-branch }}
          ci-ssh-key: ${{ secrets.CONFIG_SSH_PRIVATE_KEY }}
      - name: Notify Slack
        if: failure()
        uses: 8398a7/action-slack@v3
        with:
          status: failure
          text: FAILED Rail-history Deploy
          fields: repo, job, took
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK }}
